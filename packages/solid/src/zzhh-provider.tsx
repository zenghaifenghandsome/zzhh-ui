/* eslint-disable solid/reactivity */
import {
    
} from "solid-js";

import {
    ColorMode,
} from "./color-mode"
import {} from "./styled-system/types"

/**
 *  zzhh ui theme override configuration.
 */
export interface ZzhhThemeConfig{
    initialColorMode?: ColorMode,
    lightTheme?: Sti
}